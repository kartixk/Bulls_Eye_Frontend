# Bulls Eye Frontend

Next.js frontend for Bulls-eye.

## Setup

```bash
npm install
cp .env.local.example .env.local
npm run dev
```

The app runs at `http://localhost:3000`.

## Build

```bash
npm run build
```

## Environment

Use `.env.local.example` as the reference for required public frontend variables.
